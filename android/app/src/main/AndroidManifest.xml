<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mithila.academy">

    <!-- Required Android Permissions for Native Features -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MithilaAcademy">

        <!-- Main Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.MithilaAcademy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Widget and Deep Link Action Handlers -->
            <intent-filter>
                <action android:name="com.mithila.academy.action.SEARCH" />
                <action android:name="com.mithila.academy.action.VOICE" />
                <action android:name="com.mithila.academy.action.CAMERA" />
                <action android:name="com.mithila.academy.action.GALLERY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- REAL ANDROID HOME-SCREEN APP WIDGET PROVIDER RECEIVER -->
        <!-- Appears in the phone's Android Widgets list: Mithila Academy AI -->
        <receiver
            android:name=".widget.MithilaAppWidgetProvider"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/mithila_app_widget_info" />
        </receiver>

    </application>

</manifest>
