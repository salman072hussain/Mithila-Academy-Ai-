<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@drawable/widget_background_shape">

    <!-- Top Row: Academy Brand + Title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <ImageView
            android:id="@+id/widget_logo"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_school_amber"
            android:contentDescription="@string/app_name" />

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="@string/app_name"
            android:textColor="@color/text_primary"
            android:textSize="13sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Surendra Sir"
            android:textColor="@color/accent_amber"
            android:textSize="10sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Center Google-Style Rounded Search Bar / Tap Area -->
    <LinearLayout
        android:id="@+id/widget_search_bar_area"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="12dp"
        android:paddingEnd="6dp"
        android:background="@drawable/widget_search_pill_shape">

        <!-- Search Icon + Search Area -->
        <ImageView
            android:id="@+id/widget_search_icon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_search_amber"
            android:contentDescription="@string/shortcut_search" />

        <TextView
            android:id="@+id/widget_search_hint"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="@string/search_hint"
            android:textColor="@color/text_secondary"
            android:textSize="13sp" />

        <!-- 4 ACTION SHORTCUTS (Search, Voice, Camera, Gallery) -->

        <!-- Gallery Shortcut -->
        <ImageButton
            android:id="@+id/widget_btn_gallery"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_gallery_indigo"
            android:contentDescription="@string/shortcut_gallery" />

        <!-- Camera Shortcut -->
        <ImageButton
            android:id="@+id/widget_btn_camera"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_camera_amber"
            android:contentDescription="@string/shortcut_camera" />

        <!-- Voice Shortcut -->
        <ImageButton
            android:id="@+id/widget_btn_voice"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_mic_amber"
            android:contentDescription="@string/shortcut_voice" />

    </LinearLayout>

</LinearLayout>
